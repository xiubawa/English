# TOEIC 700 Training

This is a static GitHub Pages version of the TOEIC 700 offline training app.

Open `index.html` through GitHub Pages after deployment.

Learning progress is saved in the browser on each device with localStorage. It does not sync between devices unless cloud sync is added later.

